steps:
  # ============================================================
  # API: ビルド → プッシュ → デプロイ
  # ============================================================
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-api', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-api']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'haraka-api'
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-api'
      - '--region=asia-northeast1'
      - '--platform=managed'
      - '--no-cpu-throttling'
      - '--timeout=300'
      - '--memory=1Gi'

  # ============================================================
  # Job: ビルド → プッシュ → Cloud Run Jobs 更新
  # ============================================================
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'packages/job/Dockerfile'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'update'
      - 'haraka-sync'
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job'
      - '--region=asia-northeast1'
      - '--task-timeout=1800'
      - '--memory=2Gi'
      - '--set-env-vars=JOB_NAME=sync,TRIGGER=scheduler'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'update'
      - 'haraka-generate'
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job'
      - '--region=asia-northeast1'
      - '--task-timeout=3600'
      - '--memory=2Gi'
      - '--set-env-vars=JOB_NAME=generate,TRIGGER=scheduler'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'update'
      - 'haraka-watchdog'
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job'
      - '--region=asia-northeast1'
      - '--task-timeout=5400'
      - '--memory=2Gi'
      - '--set-env-vars=JOB_NAME=watchdog,TRIGGER=watchdog'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-api'
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/haraka/haraka-job'
